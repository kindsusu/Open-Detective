# Workflow diagram provenance

The workflow describes Open-Detective at source revision [`e387e1ba454efe175fca0afffa78e4aa158a589e`](https://github.com/kindsusu/Open-Detective/tree/e387e1ba454efe175fca0afffa78e4aa158a589e). It is an authored operating guide, not a trace of a live investigation or a claim that every step runs automatically.

The interactive artifact is generated by [Archify](https://github.com/tt-a1i/archify), version `2.17.0-dev.1`, pinned to revision `10722002bb8777ecb639d93c49586fae4adf3ae4`. The renderer was used from a local checkout; no global skill installation is required to view the HTML. English is the primary authored and viewer language. Korean guidance is provided in the repository's Korean README.

## Theme update (dark operating default)

The delivered HTML is a reviewed, reproducible themed derivative of the frozen Archify delivery. `theme_workflow_dark.py` records guards for the limited postprocess: GitHub-dark surfaces (`#0d1117`, `#161b22`, `#30363d`, `#e6edf3`, `#8b949e`), Open-Detective sky (`#8cbdd9`) and copper (`#c58b61`) accents, a dark default independent of operating-system preference, and a dark-pinned SVG export. It does not change the authored JSON topology, text, or viewer features. `?theme=light` and a saved `archify-theme=light` reader selection remain supported.

The current HTML, SVG, and PNG hashes in `VALIDATION.json` are hashes of this themed derivative and its actual viewer-button exports; they supersede the earlier vanilla delivery hashes for those files. The frozen JSON remains byte-identical. The PNG and SVG were regenerated by `_local/archify-evidence/export.py` with `?theme=dark` through the viewer's **Export → SVG / PNG** controls. `theme_check.py` verifies dark startup under a simulated light OS scheme and both explicit light overrides, then captures `open-detective.workflow.dark.png` for visual review.

## Reproduce

Check out the pinned Archify revision, then run these commands from its `archify` directory with Node.js 18 or newer. Replace the paths with your local output paths.

```bash
node bin/archify.mjs validate workflow /path/to/open-detective.workflow.json --quality showcase --json
node bin/archify.mjs deliver workflow /path/to/open-detective.workflow.json /path/to/open-detective.workflow.html --quality showcase --json
node bin/archify.mjs visual-check /path/to/open-detective.workflow.html --json
```

Validation checks the authored artifact. Browser checks inspect the delivered HTML. Neither proves that organization assets were discovered, that exposure occurred, or that a finding was resolved.

## Attribution

The generated HTML contains Archify viewer code under the MIT license: see [ARCHIFY-LICENSE.txt](ARCHIFY-LICENSE.txt). The embedded JetBrains Mono font is covered by the SIL Open Font License: see [JetBrainsMono-OFL.txt](JetBrainsMono-OFL.txt). These third-party terms remain applicable independently of the repository's own license. No third-party product logo is needed for this workflow.

## Reading and validation notes

The authored diagram contains the main operating sequence and a separately authorized local-forensics branch. Recheck and defer/reopen decisions are explained in `../WORKFLOW.md`; the drawing is not an exhaustive state machine. Node colors are renderer categories, not proof that a node is a deployed cloud service or an autonomous agent.

The frozen workflow passed all nine Archify showcase artifact checks with zero composition errors and warnings. The automated browser check did not pass its first-screen containment requirement: the complete diagram requires normal vertical scrolling at the tested desktop sizes. It was not hidden or clipped to manufacture a pass. An image-capable reviewer inspected the complete 1440-pixel-wide page and confirmed the themed main sequence and local-forensics branch are visible when scrolling. This is a qualified visual review, not a first-screen browser acceptance.

## Open-Detective dark theme derivative

The published HTML is a reviewed theme derivative of the frozen Archify output. Its dark background is `#0d1117`, panels use `#161b22`, and the primary brand accents are sky `#8cbdd9` and copper `#c58b61`. New visitors default to dark independently of OS preferences; explicit URL and saved theme choices still take precedence. SVG export captures the selected theme instead of following the reader's OS preference. PNG and SVG were exported through the actual viewer in dark mode.

The reproduction commands above rebuild the original renderer output, not this themed derivative. Preserve the committed theme changes when regenerating. `python docs/workflow/check_dark_theme.py` checks the required theme markers; it does not apply a patch. `VALIDATION.json` separates the original nine artifact checks from the derivative's browser checks and current file hashes. The diagram topology and authored text are unchanged.
